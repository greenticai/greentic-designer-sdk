name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch: {}

permissions:
  contents: write
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-binaries:
    name: Build gtdx binaries (6 platforms)
    uses: greenticai/.github/.github/workflows/release-binaries.yml@main
    with:
      package: greentic-ext-cli
      binary: gtdx
      windows-archive-format: zip
    secrets: inherit

  publish-crates:
    name: Publish to crates.io
    needs: [release-binaries]
    permissions:
      contents: read
    uses: greenticai/.github/.github/workflows/crates-publish.yml@main
    with:
      crates: "greentic-ext-contract greentic-ext-state greentic-ext-registry greentic-ext-testing greentic-ext-cli"
    secrets: inherit
