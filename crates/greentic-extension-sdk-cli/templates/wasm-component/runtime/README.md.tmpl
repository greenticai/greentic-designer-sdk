# Runtime artifact

The scaffold includes `example.gtpack` so `describe.json` validates immediately.
Replace it with your real pre-built `.gtpack`, or run `gtdx dev` to have one
built and signed locally.

The expected filename is referenced from `../describe.json` under
`runtime.gtpack.file`. Update that field if you choose a different name.
