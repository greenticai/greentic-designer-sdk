# {{name}}

A Greentic Designer artifact-producing DesignExtension.

- id: `{{id}}`
- version: `{{version}}`
- contract: `greentic:extension-design@{{contract_version}}`

The `generate_artifact` tool returns the generic generated artifact output shape documented by the SDK. The checked-in `examples/artifact-output.json` file is a static sample that can be validated by tests and host integrations without executing the WASM component.

## Develop

```
gtdx dev
```

## Publish

```
gtdx publish
```
